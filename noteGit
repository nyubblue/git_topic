git remote add origin .....namebprojcet.git

git push -u origin <name branch>


1. Create ssh key:  **ssh-keygen -t ed25519 -C "your_email@example.com"**
or Generating a new SSH key for a hardware security key : **$ ssh-keygen -t ed25519-sk -C "your_email@example.com"**
2. Auto-launching the ssh-agent
$ **eval "$(ssh-agent -s)"**
> Agent pid 59566
3. Adding your SSH key to the ssh-agent
**ssh-add ~/.ssh/id_ed25519**

**id_ed25519** is key that you entered when generating ssh key
If you entered key = "ssh_key" then you must copy to ssh_key.pub to ~/.ssh/
Or you entered key = "/c/Users/you/.ssh/id_algorithm" then you don't need to copy to ~/.ssh/ 
